apply plugin: 'application'

description 'directory listener for auto image capture'

mainClassName = "org.icepdf.os.examples.capture.DocumentCapture"
applicationDefaultJvmArgs = ["-Xms64m", "-Xmx1024m"]

dependencies {
    compile project(':viewer:viewer-awt')
}
