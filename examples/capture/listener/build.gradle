plugins {
    id 'java'
    id 'application'
}

dependencies {
    implementation project(':core:core-awt'), project(':viewer:viewer-awt')
}

description = 'page capture with listener callbacks'

application {
    mainClass = "org.icepdf.examples.capture.DocumentCapture"
    applicationDefaultJvmArgs = ["-Xms64m", "-Xmx1024m"]
}
