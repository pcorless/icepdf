plugins {
    id 'java'
    id 'application'
}

dependencies {
    implementation project(':core:core-awt'), project(':viewer:viewer-awt')
}

description 'directory listener for auto image capture'

mainClassName = "org.icepdf.examples.capture.DocumentCapture"
applicationDefaultJvmArgs = ["-Xms64m", "-Xmx1024m"]
