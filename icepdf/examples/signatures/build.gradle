apply plugin: 'application'

description 'signature validation example'

mainClassName = "org.icepdf.os.examples.signatures.SignatureVerification"
applicationDefaultJvmArgs = ["-Xms64m", "-Xmx1024m"]

dependencies {
    compile project(':viewer:viewer-awt')
}
