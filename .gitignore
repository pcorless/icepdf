# Gradle build output (exclude)
/.gradle/
/build/
**/target/

# IDE files (exclude)
/.idea/
*.iml

# Gradle wrapper files (COMMIT THESE!)
# gradlew - executable script (commit)
# gradlew.bat - Windows script (commit)
# gradle/wrapper/ - wrapper JAR and properties (commit)

